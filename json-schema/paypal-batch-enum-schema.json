{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/batch_enum",
  "title": "Batch status",
  "description": "The payouts status.",
  "type": "string",
  "minLength": 1,
  "maxLength": 36,
  "pattern": "^[0-9A-Z_]+$",
  "enum": [
    "DENIED",
    "PENDING",
    "PROCESSING",
    "SUCCESS",
    "CANCELED"
  ]
}